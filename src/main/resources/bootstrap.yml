spring:
  application:
    name: eureka
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
      failFast: false

---

spring:
  profiles: docker
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://config:8888}
      failFast: true